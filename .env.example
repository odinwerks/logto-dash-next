# Logto Configuration for Debug Dashboard
APP_ID=your_app_id_from_logto_admin
APP_SECRET=your_app_secret_from_logto_admin
ENDPOINT=https://your-logto-instance.com
BASE_URL=http://localhost:3000
COOKIE_SECRET=generate_a_32_character_random_string_here
NODE_ENV=development

# Optional: Additional scopes beyond default (profile, custom_data, email, phone, identities)
# SCOPES=organizations,organization_roles

# Note: The dashboard requires Logto Account API to be enabled at ${ENDPOINT}/api/my-account
# Ensure your Logto instance has the Account API feature enabled